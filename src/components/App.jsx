import { BrowserRouter, Route, Routes } from 'react-router-dom';

function App() {
  return (
    <div className='App'>
      <BrowserRouter>
        <Routes>
          {/* <Route path='/' element={<Box />} /> */}
        </Routes>
      </BrowserRouter>
    </div>
  );
}

export default App;
